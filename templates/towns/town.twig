{% extends '_layout' %}

{% block content %}
    <h1>{{ entry.title }}</h1>


    <img src="{{ entry.townImage.one().url }}" alt="Photo of {{ entry.title }}" role="presentation"/>

    <p>
    {{ entry.townDescription }}
    </p>
{% endblock %}


{% extends '_layout' %}

{% block content %}
    <section class="py-8 lg:py-16">
        <article class="prose lg:prose-xl mx-auto px-4 lg:px-0">
        <h1 class="pb-4 lg:pb-8">{{ entry.title }}</h1>
        {{ entry.post_content }}
        </article>
    </section>
{% endblock %}